<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nomapp.nomapp_beta">

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activities.StartActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.AddIngridientsActivity"
            android:label="Add Ingridient"></activity>
        <activity
            android:name=".Activities.ListOfAvaliableRecipesActivity"
            android:label="Available recipes"></activity>
        <activity
            android:name=".Activities.RecipeActivity"
            android:label="Steps"></activity>
        <activity
            android:name=".Activities.SettingsActivity"
            android:label="Steps"></activity>
        <activity android:name=".Activities.TabsActivity"
            android:label="Steps"></activity>
        <activity android:name=".Activities.CategoriesActivity"
            android:label="Steps"></activity>
        <activity android:name=".Activities.RecipePreviewActivity"
            android:label="Preview"></activity>

    </application>


</manifest>
